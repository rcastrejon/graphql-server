{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --tree-kill --no-notify --respawn --transpile-only src/index"
  },
  "devDependencies": {
    "@types/node": "^14.14.7",
    "ts-node-dev": "^1.0.0",
    "typescript": "^4.0.5"
  }
}
